# Python binding configuration file.

add_custom_target(run_swig ALL swig -python -py3 -c++ ${CMAKE_CURRENT_SOURCE_DIR}/scge.i)

add_custom_target(rename_lib rename(${CMAKE_CURRENT_SOURCE_DIR}/libengine_python.so, ${CMAKE_CURRENT_SOURCE_DIR}/_scge.so)
	DEPENDS engine_python)

set(LIBRARY_OUTPUT_PATH ${CMAKE_CURRENT_SOURCE_DIR})
project(engine_python)
include_directories("/usr/include/python3.1/")

add_library(engine_python SHARED ../engine/engine.cpp scge_wrap.cxx)#../engine/engine.cpp 
add_definitions(-c -x c++ -fPIC)
target_link_libraries(engine_python engine ${LIBRARY_LIST})
#swig -python -py3 -c++ scge.i && gcc -c -x c++ -fPIC ../engine/engine.cpp scge_wrap.cxx -I/usr/include/python3.1 && gcc -shared engine.o scge_wrap.o -lglfw -lGLEW -lSOIL -lfreetype -lftgl -lvorbisfile -lalure -lopenal -lenet -o _scge.so
